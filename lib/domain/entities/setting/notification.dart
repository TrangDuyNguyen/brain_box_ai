class NotificationEntity {
  // common notification setting
  final bool isEnable;
  final bool isSound;
  final bool isVibrate;
  final bool isLight;

  NotificationEntity({
    required this.isEnable,
    required this.isSound,
    required this.isVibrate,
    required this.isLight,
  });
}
